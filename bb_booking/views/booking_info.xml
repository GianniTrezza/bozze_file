<odoo>
    <record id="view_account_move_booking_form_inherit" model="ir.ui.view">
        <field name="name">account.move.booking.form.inherit</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            

<!--


            <field name="createTime" position="after">
                <field name="updateTime"/>
            </field>

             Field ralativo a data check in/out
             -->
            <field name="partner_id" position="after">
                <field name="refer"/>
            </field>

            <field name="refer" position="after">
                <field name="checkin"/>
            </field>

            <field name="checkin" position="after">
                <field name="checkout"/>
            </field>

             <field name="checkout" position="after">
                <field name="totalGuest"/>
            </field>

            <field name="payment_reference" position="after">
                <field name="rooms"/>
            </field>

            <field name="rooms" position="after">
                <field name="roomGross"/>
            </field>

            <!-- <xpath expr="//page[@name='invoice_tab']/field[@name='invoice_line_ids']/tree" position="inside">
                <field name="name" string="Identificativo della prenotazione"/>
                <field name="quantity" string="Numero stanze"/>
                <field name="price_unit" string="Costo stanza"/>
                <field name="account_id" string="Account ID"/>
            </xpath> -->
            <xpath expr="//page[@name='invoice_tab']/field[@name='invoice_line_ids']/tree/field[@name='analytic_distribution']" position="replace">
                <field name="price_unit" string="Costo stanza"/>
            </xpath>
        </field>
    </record>





            <!--modifica dei campi del model "account.move.line"-->
            <!-- <xpath expr="//page[@name='invoice_tab']" position="attributes">
                <attribute name="string">Dettagli prenotazione</attribute>
            </xpath>
            <xpath expr="//page[@name='invoice_tab']/field[@name='invoice_line_ids']/tree/field[@name='name']" position="after">
                <field name="tax_ids" widget="many2many_tags" optional="show"/>
            </xpath> -->
            <!-- <xpath expr="//page[@name='invoice_tab']/field[@name='invoice_line_ids']/tree/field[@name='account_id']" position="replace"/> -->
            <!-- <xpath expr="//page[@name='invoice_tab']/field[@name='invoice_line_ids']/tree/field[@name='analytic_distribution']" position="replace"/> -->

        <!-- </field>
    </record> -->

            

            <!-- <xpath expr="//page[@name='invoice_tab']/field[@name='invoice_line_ids']/tree" position="inside">
                <field name="refer"/>
                <field name="checkin"/>
                <field name="checkout"/>
                <field name="totalGuest"/>
            </xpath> -->
<!--

            <xpath expr="//page[@name='invoice_tab']/field[@name='invoice_line_ids']/tree/field[@name='name']"
                   position="replace">
                <field name="channelNotes"/>
            </xpath>
            <xpath expr="//page[@name='invoice_tab']/field[@name='invoice_line_ids']/tree/field[@name='quantity']"
                   position="replace">
                <field name="rooms"/>
            </xpath>
            <xpath expr="//page[@name='invoice_tab']/field[@name='invoice_line_ids']/tree/field[@name='price_unit']"
                   position="replace">
                <field name="roomGross" editable="top"/>
            </xpath>

            <xpath expr="//page[@name='invoice_tab']/field[@name='invoice_line_ids']/tree/field[@name='price_subtotal']"
                   position="replace">
                <field name="totalGross"/>
            </xpath>
            -->
        

    <record id="FATTURE_SEQUENCE" model="ir.sequence">
       <field name="name">Sequenza</field>
       <field name="code">account.move</field>
       <field name="prefix">ACCR/%(year)s/</field>
       <field name="number_increment" eval="1"/>
    </record>

</odoo>

<!-- NEW CODE -->
<!-- <odoo>
    <record id="view_account_move_booking_form_inherit" model="ir.ui.view">
        <field name="name">account.move.booking.form.inherit</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">

            <xpath expr="//page[@name='invoice_tab']" position="replace">
                <page string="Informazioni fattura">
                    <field name="invoice_line_ids" widget="section_and_note_one2many" mode="tree,kanban">
                        <tree editable="bottom">
                            <field name="product_id"/>
                            <field name="name"/>
                            <field name="price_unit" string="Price"/>
                        </tree>
                    </field>
                </page>
            </xpath>

            <field name="partner_id" position="after">
                <field name="refer"/>
            </field>

            <field name="refer" position="after">
                <field name="checkin"/>
            </field>

            <field name="checkin" position="after">
                <field name="checkout"/>
            </field>

            <field name="checkout" position="after">
                <field name="totalGuest"/>
            </field>

            <field name="payment_reference" position="after">
                <field name="rooms"/>
            </field>

            <field name="rooms" position="after">
                <field name="roomGross"/>
            </field>

        </field>
    </record>
</odoo> -->
